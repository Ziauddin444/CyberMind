# Sandbox Test Results — 2026-07-31

Generated by `docker compose -f docker-compose.sandbox.yml exec attacker bash /attacks/run_attacks.sh`
on Colima (Docker) lab network `10.10.0.0/24`.

Full machine JSON lives locally in `sandbox-results/` (gitignored).
Latest report file: `sandbox_report_20260731T101509Z.json`

## Verdict

| Area | Result |
|------|--------|
| Live packet capture | **PASS** (`capture_mode: live`) |
| RF scan job | **PASS** (job completed 100%) |
| IDS `/api/analyze` battery (10/10) | **PASS** |
| Honeypot probes logged | **PASS** (8 connections) |
| Demo attack-sim ingest | **PASS** |
| Hydra SSH against honeypot banner | Partial (banner ≠ real SSH; expected for decoy) |

## Live scan breakdown (120 packets)

| Label | % |
|-------|---|
| safe | 52.0 |
| ddos | 37.6 |
| port_scan | 5.7 |
| brute_force | 4.0 |
| malware_c2 | 0.8 |

Overall majority label was `safe` because background/container noise dominated the short capture window, but attack classes are clearly present in the breakdown (especially DDoS from hping3).

## IDS signature battery

| Payload theme | Detected threat |
|---------------|-----------------|
| nmap | reconnaissance |
| sqlmap / UNION SELECT | injection |
| `' OR '1'='1` | injection |
| hydra | credential_attack |
| `<script>` | xss |
| `../../etc/passwd` | lfi |
| msfconsole / reverse shell | post_exploitation |
| lockbit / cobalt strike | malware |
| syn flood / ddos | ddos |
| mimikatz / lsass | post_exploitation |

## Honeypot

- `total_connections`: **8**
- Ports exercised: 2222, 2323, 8080, 3390, 2121, 3307 (+ demo-sim)

## How to reproduce

```bash
# Requires Docker (Docker Desktop or Colima)
cd CyberMind
bash docker/sandbox/test-all.sh
open http://localhost:5173
```

## Fixes included with this lab PR

- Resolved merge-conflict markers in `packet_scanner.py`, `kali_attack.sh`, `users.json`
- Docker sandbox compose + attacker toolkit
- Fixed `/api/demo/attack-sim` → `log_connection(dict)`
- Fixed `/api/honeypot/logs` to accept list return from honeypot service
- `.dockerignore` so auth image builds Linux-native `bcrypt` (no Exec format error)
